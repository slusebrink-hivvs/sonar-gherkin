# must be unique in a given SonarQube instance
sonar.projectKey=sonar-gherkin-plugin
# this is the name displayed in the SonarQube UI
sonar.projectName=SonarQube Gherkin (Cucumber) Plugin
sonar.projectVersion=0.3

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=./src,./features

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.java.source=8

# Doesn't check the included bsl files
# sonar.exclusions=**/*.something, **/*.anothersomething

sonar.junit.reportsPath=target/surefire-reports
sonar.jacoco.itReportPath=target/coverage-reports/jacoco-it.exec
sonar.java.binaries=target/classes,target/test-classes
